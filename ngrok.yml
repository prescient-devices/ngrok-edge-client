version: "2"
authtoken: <add_your_token_here>
tunnels:
  ssh:
    proto: tcp
    addr: 22
    metadata: '{"devID": "device_id", "comment": ""}'
  # nodered:
  #   proto: http
  #   addr: 1881
  #   auth: "pdi:abcdef" #for adding http authentication
  #   bind_tls: true
